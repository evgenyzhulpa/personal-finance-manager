version: '3'
services:
  postgres:
    image: postgres:12.3
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=1q2w3e4r
      - POSTGRES_DB=app.db
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

